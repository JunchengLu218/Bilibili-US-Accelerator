# Changelog

本文件记录用户可感知的功能、兼容性和文档变化。仓库早期的固定 CDN 稳定插件与手动测速实验插件都曾使用 `0.1.0`，因此下面将“稳定发布线”和“测速功能迭代线”分开列出。

## 稳定发布线

### 0.1.8 - 2026-08-12

- 将稳定版从固定改写单一 `mirrorali`，升级为捕获真实分片后手动测试 8 个普通 UPOS 候选。
- 稳定版固定 `Mode=manual`，普通播放请求不会等待测速；结果只在用户运行节点菜单后写入缓存。
- 保留普通 UPOS、BStar、PCDN、MCDN、Akamai 和直播的独立分类与安全开关。
- 使用蓝色无透明外边框图标，兼容 Loon `3.5.0(969)`。
- GitHub Release 同时发布稳定手动版和实验自动版；Release 资产固定引用版本 tag，不再随 `main` 后续变化。

### 0.1.0 - 2026-08-03

- 首次发布固定 CDN 稳定插件。
- 将普通 Bilibili UPOS/HK 视频 CDN 固定到 `upos-sz-mirrorali.bilivideo.com`。
- 保留原始协议、路径、Range 和查询参数。
- 排除 Akamai 改写，避免 `os=akam` / `hmac` 分片跨 CDN 后发生花屏。
- 确认兼容 Loon 3.5.0 的旧式 `header` Rewrite 语法。

## 测速功能迭代线

### 0.2.1 - 2026-08-12（实验自动版）

- 将自动测速默认池从 4 个扩展到 8 个，与稳定手动版使用相同候选列表。
- 实验插件固定 `Mode=first-request`；无有效缓存时自动测试并改写当前请求，失败始终原路放行。
- 将四个可触发测速的 HTTP 入口和 Generic 入口超时提高到 180 秒，覆盖 `8 候选 × 3 轮 × 5 秒` 的最大配置。
- 实验版改用黑底粉色电视标志，稳定版继续使用蓝色图标。
- 更新测试插件生成器、候选池回归测试和 Release 资产验证。
- 完整整理 README 的安装步骤、全部参数、流量估算、验证方法和图标维护说明。

### 0.2.0 - 2026-08-07（自动模式首次测试）

- 在手动流程之外新增可选 `first-request` 模式，初始使用 4 个候选。
- 无有效缓存时，第一条合格请求会串行测速并在成功后改写同一条请求。
- 增加并发测速锁，避免视频和音频分片重复启动 Probe。
- 增加自动失败 5 分钟冷却；全部失败、锁被占用或保存失败时原路放行。
- 自动与手动模式共用安全验证，但按网络、请求类型和设置指纹隔离结果。

### 0.1.7 - 2026-08-07（8 候选手动测试）

- 手动候选池从 4 个扩展到 8 个：3 个海外镜像和 5 个常规节点。
- Generic 总超时从 120 秒提高到 180 秒。
- 增加默认候选池回归测试，确认插件参数和脚本内置列表一致。

### 0.1.6 - 2026-08-06 / 2026-08-07

- 按请求 profile 计算设置指纹：普通 UPOS、BStar、PCDN、MCDN 和 Akamai 只受各自相关开关影响。
- 修复无关设置变化导致有效普通 UPOS 缓存被错误判定失效的问题。
- 将网络、profile 和设置指纹同时写入缓存诊断信息，便于解释 `settings-mismatch`。
- 增加仓库托管的蓝色插件图标，并让测试插件为图标 URL 添加分支和缓存破坏参数。

### 0.1.5 - 2026-08-06

- 捕获视频分片时同时保存规范化、无敏感信息的设置快照。
- Generic 手动测速优先复用捕获时的设置，解决 Loon 3.5.0 在 HTTP 请求入口和 Generic 入口暴露参数不一致的问题。
- 对设置快照增加版本和指纹验证，无法安全恢复时拒绝误用结果。

### 0.1.4 - 2026-08-06

- 增加测速缓存交接诊断，区分缺少缓存、设置不一致、网络不一致、profile 不一致和结果过期。
- 结果通知显示缓存网络与设置指纹，便于确认测速和播放请求是否处于同一环境。
- 修复测速完成但后续请求没有应用结果时缺少可观察信息的问题。

### 0.1.3 - 2026-08-06

- Generic `$done()` 增加 `title` 和 `htmlMessage`，在运行结果页直接显示排名、无样本、全部失败和异常信息。
- 不再让成功测速只表现为 `empty content`。
- 清理重复的 Generic 完成消息字段，保持 Loon 3.5.0 结果页输出稳定。

### 0.1.2 - 2026-08-06

- 将 Generic 菜单图标改为 Loon 3.5.0 可识别的 `atom.system` SF Symbol 格式。
- 保留 `enable=true`，修复节点长按菜单中看不到测速入口的问题。

### 0.1.1 - 2026-08-06

- 首次为 Generic 测速入口补充 `img-url` 和 `enable=true`，使操作可以注册到节点长按菜单。
- 同步更新测试插件和验证脚本，检查 Generic 入口元数据。

### 0.1.0 - 2026-08-05（手动测速初版）

- 新增捕获真实 Bilibili 分片、手动串行测速和后续缓存改写的实验插件。
- 初始使用 4 个普通 UPOS 候选；不阻塞第一次播放。
- 建立普通 UPOS、BStar、PCDN、MCDN、Akamai 和直播的分类顺序。
- 候选严格限制为普通 Bilibili UPOS/HK Host；保留原始签名路径和查询参数。
- Probe 使用固定 Range，并要求正确 `206`、`Content-Range` 与二进制长度。
- 全部失败或不安全时不保存结果，播放请求原路放行。
- 添加测试分支插件生成器、Node.js 测试和初版详解文档。
